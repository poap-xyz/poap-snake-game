{
  "name": "poap-snake",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.0",
    "@babel/preset-react": "^7.16.7",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "dist": "rm -rf dist && mkdir dist && babel src -d dist --ignore 'src/index.js,src/Demo.js' --copy-files --no-copy-ignored"
  },
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
